[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Use native Next.js support - NO plugin
[settings]
  # Disable Edge Functions for now
  # This ensures proper static file serving
